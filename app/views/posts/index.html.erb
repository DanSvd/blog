<h1>All articles</h1>

<ul>
  <% @posts.each do |post| %>
    <p>Title :<%= link_to post.title, post_path(post) %> </p>
    </%>
    <p>Category :<%= post.category%></p>
      <% end %>
    <ul>
</ul>

<%= link_to 'Back', root_path %>


